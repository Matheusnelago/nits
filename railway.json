{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python3 -m pip install -r backend/requirments.txt",
    "installCommand": "python3 -m pip install -r backend/requirments.txt"
  },
  "deploy": {
    "numReplicas": 1,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "cd backend && python3 -m gunicorn core.wsgi:application --bind 0.0.0.0:$PORT --workers 2",
    "healthcheckPath": "/api/",
    "healthcheckTimeout": 30,
    "environmentVariables": {
      "DEBUG": "False",
      "SECRET_KEY": "your-secret-key-change-in-production",
      "ALLOWED_HOSTS": "your-app-name.railway.app"
    }
  }
}

